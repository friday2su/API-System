{
    "name": "api-system-root",
    "private": true,
    "author": "Friday (friday2su)",
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "install-server": "cd server && npm install"
    },
    "dependencies": {
        "express": "^4.18.2",
        "cors": "^2.8.5",
        "axios": "^1.6.2"
    }
}